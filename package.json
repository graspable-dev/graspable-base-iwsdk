{
  "name": "@graspable-dev/webxr-base-iwsdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fresh:install": "npx rimraf package-lock.json node_modules && npm install",
    "fresh:ci": "npx rimraf node_modules && npm ci",
    "fresh:dev": "npm run fresh:install && npm run dev",
    "fresh:build": "npm run fresh:install && npm run build"
  },
  "dependencies": {
    "@iwsdk/core": "^0.1.0",
    "three": "npm:super-three@0.177.0"
  },
  "devDependencies": {
    "@iwsdk/vite-plugin-gltf-optimizer": "^0.1.0",
    "@iwsdk/vite-plugin-iwer": "^0.1.0",
    "@iwsdk/vite-plugin-metaspatial": "^0.1.0",
    "@iwsdk/vite-plugin-uikitml": "^0.1.0",
    "vite": "^6.3.1",
    "vite-plugin-mkcert": "^1.17.0"
  }
}
